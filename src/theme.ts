import { createTheme } from '@mui/material/styles';
const theme = createTheme({
  palette: {
    primary: { main: '#2b7a78' },
    secondary: { main: '#3aafa9' },
    background: { default: '#f0f6f6' }
  },
});
export default theme;